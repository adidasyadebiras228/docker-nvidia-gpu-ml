### `docs/troubleshooting.md`

Include fixes for:

* `docker: permission denied`
* `could not select device driver "" with capabilities: [[gpu]]`
* `nvidia-smi` works on host but not container
* Secure Boot / driver mismatch notes (brief)
